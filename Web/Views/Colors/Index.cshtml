@model Color_VM

@{
    ViewBag.Title = "Renkler";
}

<filters>
    <filter for="Name" placeholder="Renk ara..." operator="contains" type="text" />
</filters>

<grid read-url="/Colors/Read" edit-url="/Colors/Update" delete-url="/Colors/Delete" create-url="/Colors/Create">
    <column custom-property="true" sortable="false"></column>
    <column for="Name"></column>
    <column for="CreatedDate"></column>
</grid>

