@using Entities.Dtos.Categories
@model CategoryDto

@{
    ViewBag.Title = "Details";
}

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>
                @Html.DisplayFor(modelItem => modelItem.Name)
                <input type="hidden" id="id" category-id="@Model.Id" />
            </td>
            <td>
                <a asp-action="Delete" asp-route-id="@Model.Id">Delete</a>
                <a asp-action="Update" asp-route-id="@Model.Id">Edit</a>
            </td>
    </tbody>
</table>

@section scripts{
    <script>
        debugger;
        var id = $("id").attr('category-id');
        debugger;
        $.ajax({
            url: '/category/get',
            data: { Id: id },
            type: 'get',
            dataType: 'json',
            success: function (response) {
                debugger;
                // Sayfa içeriğini güncelleme işlemleri
            },
            error: function () {
                // Hata işlemleri
            }
        });

    </script>
}